<header class="primary">
  <div><%= image_tag "logo.svg", alt: "Photo Gallery", class: "logo" %> <h1 class="app-name">Photo Gallery</h1></div>
  
  <% if user_signed_in? %>
    <div><%= link_to "Log Out", destroy_user_session_path, class: "logout", data: { turbo_method: :delete } %></div>
  <% end %>
</header>