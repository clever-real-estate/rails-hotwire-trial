<div>
  <img
    src="<%= photo.image_url %>"
    alt="<%= photo.alt %>"
    title="<%= photo.alt %>"
    />
  <p><strong><%= photo.photographer %></strong></p>

  <p>
    <a href="<%= photo.source_url %>" target="_blank" >
      View Source
    </a>
  </p>

  <p>Likes: <%= photo.likes.count %></p>
</div>

<hr>